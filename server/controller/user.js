const { infoLog } = require("../helper/logHelper");
const User = require("../models/User");

// fetch user profile
const getUser = async (req, res, next) => {
    try {

    } catch (error) {
        infoLog("getUser exit")
        errorLog("Error while fetching user")
    }
}